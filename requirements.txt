# =============================================================================
# finance-copilot — requirements.txt
# Runtime dependencies (pinned for Docker reproducibility).
# Generated from pyproject.toml [project.dependencies].
# To regenerate: pip-compile pyproject.toml -o requirements.txt
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi>=0.110,<0.200
uvicorn[standard]>=0.29,<0.30

# --- Config / secrets --------------------------------------------------------
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- AI / LLM orchestration --------------------------------------------------
langchain>=0.2,<0.3
langchain-openai>=0.1,<0.2
langchain-google-genai>=1.0,<2.0
langchain-anthropic>=0.1,<0.2
langchain-text-splitters>=0.2,<0.3
langgraph>=0.1,<0.2           # multi-agent graph orchestration (LangGraph)
openai>=1.0.0                 # direct LLM client
langchain-huggingface>=0.0.1  # local embeddings (CRITICAL: DO NOT CACHE)

# --- Financial data ----------------------------------------------------------
yfinance>=0.2,<0.3            # Yahoo Finance wrapper (unofficial but widely used)
edgartools>=3.0,<4.0              # SEC EDGAR API client (rich, Pythonic interface)
pandas>=2.2,<3.0
numpy>=1.26,<2.0

# --- HTTP / scraping ---------------------------------------------------------
httpx>=0.27,<0.28
beautifulsoup4>=4.12,<5.0
lxml>=5.1,<6.0                 # fast HTML/XML parser used by bs4

# --- Outputs -----------------------------------------------------------------
python-pptx>=0.6,<1.0         # PowerPoint generation
reportlab>=4.1,<5.0           # PDF generation
jinja2>=3.1,<4.0              # templating engine
openpyxl>=3.1,<4.0            # Excel output

# --- Database (PostgreSQL) ---------------------------------------------------
sqlmodel>=0.0.16,<0.1.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0

# --- Caching / async --------------------------------------------------------
redis>=5.0,<6.0
aioredis>=2.0,<3.0

# --- Observability -----------------------------------------------------------
structlog>=24.1,<25.0
opentelemetry-sdk>=1.24,<2.0
opentelemetry-exporter-otlp>=1.24,<2.0
langfuse>=2.0,<3.0             # LLM observability + tracing

# --- Utilities ---------------------------------------------------------------
python-dotenv>=1.0,<2.0       # .env file loading outside Docker
tenacity>=8.2,<9.0            # retry logic for external API calls

# --- Vector Database & Local Embeddings ---
chromadb==0.5.20
sentence-transformers>=3.0,<4.0
torch>=2.2,<3.0 --index-url https://download.pytorch.org/whl/cpu # Default to CPU for safe builds, GPU can be enabled via environment.

# --- Image Processing ---
Pillow>=11.0.0
